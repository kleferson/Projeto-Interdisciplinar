<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
    integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js"
    integrity="sha384-wfSDF2E50Y2D1uUdj0O3uMBJnjuUD4Ih7YwaYd1iqfktj0Uod8GCExl3Og8ifwB6"
    crossorigin="anonymous"></script>
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js" type="text/javascript"></script>
</head>

<body>
  <div class="side-bar">
        <h1>Logo</h1>
        <div class="bar">
            <a href="/" class="material-icons">home</a>
            <a href="/showprontuarios" class="material-icons">group</a>
            {{#if user.admin}}
            <a href="/admin" class="material-icons">admin_panel_settings</a>
            <a href="/admin/cadastrar-usuario" class="material-icons" id="selecionado">account_box</a>
            {{/if}}
            <a href="#" class="material-icons">settings</a>
        </div>
        <div class="logout">
            <a href="/logout" class="material-icons">exit_to_app</a>
        </div>
    </div>

  <div class="nav-bar">
    <div class="description">
      <h1>Usuários</h1>
    </div>
    <div class="user-info">
      <span>Olá,<b> {{user.nome}}</b></span>
    </div>
  </div>

  <div class="main">
    <div class="main-user">
      <form action="/admin/cadastrar-usuario/newuser" method="POST">
        {{>_msg}}
        {{#each erros}}
        <div class="alert alert-danger">{{texto}}</div>
        {{else}}

        {{/each}}
        <div class="form-group">
          <div>
            <label for="">Nome de Usuário:</label>
            <input type="text" name="nome" class="form-control" id="exampleFirstName">
          </div>
        </div>
        <div class="form-group">
          <div>
            <label for="">Email:</label>
            <input type="email" name="email" class="form-control" id="exampleInputEmail">
          </div>
        </div>
        <div class="form-group">
          <div>
            <label for="">Senha:</label>
            <input type="password" name="senha" class="form-control" id="exampleInputPassword">
          </div>
        </div>
        <div class="form-group">
          <div>
            <label for="">Repita a senha:</label>
            <input type="password" name="senha2" class="form-control" id="exampleRepeatPassword">
          </div>
        </div>

        <button type="submit">Cadastrar</button>
      </form>

      <div class="teste">
        <div class="title">
          <h1>Usuários Cadastrados</h1>
        </div>
        <table class="table table-hover" id="table-users">
          <thead>
            <tr>
              <th>Usuário</th>
              <th>Email</th>
              <th></th>
            </tr>
          </thead>
          <tbody>
            {{#each usuarios}}
            <tr>
              <td>{{nome}}</td>
              <td>{{email}}</td>
              <td><a href="#"><button class="btn2">Excluir</button></a></td>
            </tr>
            {{else}}
            {{/each}}
          </tbody>
        </table>
      </div>
    </div>
  </div>
</body>

</html>