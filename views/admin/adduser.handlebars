<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
    integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js"
    integrity="sha384-wfSDF2E50Y2D1uUdj0O3uMBJnjuUD4Ih7YwaYd1iqfktj0Uod8GCExl3Og8ifwB6"
    crossorigin="anonymous"></script>
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js" type="text/javascript"></script>
</head>

<body>
  <div class="side-bar">
    <div class="bar">
      <h1>Logo</h1>
      <a href="/"><span class="material-icons">dashboard</span><br>Dashboard</a>
      {{#if user.admin}}
      <a href="/admin"><span class="material-icons">description</span><br>Editar</a>
      <a href="/admin/cadastrar-usuario" id="selecionado"><span
          class="material-icons">account_box</span><br>Usuários</a>
      {{/if}}
      <a href="/showprontuarios"><span class="material-icons">view_list</span><br>Prontuários</a>
    </div>
    <div class="logout">
      <p>{{user.nome}}</p>
      <a href="/logout"><span class="material-icons">exit_to_app</span><br>Logout</a>
    </div>
  </div>

  <div class="nav-bar">
    <input class="form-control " type="text" id="search" placeholder="Pesquisar" aria-label="Search">
    <div class="user-info">
      <span>Olá, <b>{{user.nome}}</b></span>
      <div class="divider"></div>
      <a href="/logout" data-toggle="modal" data-target="#logoutModal" class="material-icons">exit_to_app</a>
    </div>
  </div>

  <div class="main">
    <div class="main-user">
      <form action="/admin/cadastrar-usuario/newuser" method="POST">
        {{>_msg}}
        {{#each erros}}
        <div class="alert alert-danger">{{texto}}</div>
        {{else}}

        {{/each}}
        <div class="form-group">
          <div>
            <label for="">Nome de Usuário:</label>
            <input type="text" name="nome" class="form-control" id="exampleFirstName">
          </div>
        </div>
        <div class="form-group">
          <div>
            <label for="">Email:</label>
            <input type="email" name="email" class="form-control" id="exampleInputEmail">
          </div>
        </div>
        <div class="form-group">
          <div>
            <label for="">Senha:</label>
            <input type="password" name="senha" class="form-control" id="exampleInputPassword">
          </div>
        </div>
        <div class="form-group">
          <div>
            <label for="">Repita a senha:</label>
            <input type="password" name="senha2" class="form-control" id="exampleRepeatPassword">
          </div>
        </div>

        <button type="submit">Cadastrar</button>
      </form>

      <div class="teste">
        <div class="title">
          <h1>Usuários Cadastrados</h1>
        </div>
        <table class="table table-hover" id="table-users">
          <thead>
            <tr>
              <th>Usuário</th>
              <th>Email</th>
              <th></th>
            </tr>
          </thead>
          <tbody>
            {{#each usuarios}}
            <tr>
              <td>{{nome}}</td>
              <td>{{email}}</td>
              <td><a href="#"><button class="btn2">Excluir</button></a></td>
            </tr>
            {{else}}
            {{/each}}
          </tbody>
        </table>
      </div>
    </div>
  </div>
</body>

</html>