<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Início</title>
  <link rel="stylesheet" href="css/style.css">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
    integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js"
    integrity="sha384-wfSDF2E50Y2D1uUdj0O3uMBJnjuUD4Ih7YwaYd1iqfktj0Uod8GCExl3Og8ifwB6"
    crossorigin="anonymous"></script>
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js" type="text/javascript"></script>
</head>

<body>
  <div class="side-bar">
    <div class="bar">
      <h1>Logo</h1>
      <a href="/" id="selecionado"><span class="material-icons">dashboard</span><br>Dashboard</a>
      {{#if user.admin}}
      <a href="/admin"><span class="material-icons">description</span><br>Editar</a>
      <a href="/admin/cadastrar-usuario"><span class="material-icons">account_box</span><br>Usuários</a>
      {{/if}}
      <a href="/showprontuarios"><span class="material-icons">view_list</span><br>Prontuários</a>
    </div>
    <div class="logout">
      <p>{{user.nome}}</p>
       <a href="/logout"><span class="material-icons">exit_to_app</span><br>Logout</a>
    </div>
  </div>

  <div class="nav-bar">
        <div class="search">
            <h1>Dashboard</h1>
        </div>
        <div class="user-info">
            <a href="/cadastrar-prontuario"><button class="btn4">Novo Prontuário</button></a>
        </div>
    </div>

  <div class="main">
    <div class="main-dashboard">

      <div class="main-card">
        <div class="card-title">
          <p>Bem-vindo, {{user.nome}}!</p>
          <div class="card-subtitle">
            <span>Lembre-se de deixar o prontuário do seus pacientes sempre atualizados!</span>
          </div>
        </div>
        <img src="img/undraw_doctor_kw5l.svg" alt="">
      </div>


      {{#if user.admin}}
      {{>_msg}}
      {{#each erros}}
      <div class="alert alert-danger">{{texto}}</div>
      {{else}}
      {{/each}}

      <form action="/newpost" method="POST">
        <input type="hidden" name="autor" value="{{user.nome}}">
        <label for="titulo"><b>Titulo</b></label>
        <input type="text" class="form-control" name="titulo"><br>
        <label for=""><b>Texto</b></label>
        <textarea type="text" class="form-control" name="conteudo" rows="4"></textarea>
        <button type="submit">Publicar</button>
      </form>
      {{/if}}

      <div class="teste">
        {{#each postagens}}
        <div class="mural">
          <h1>{{titulo}}</h1>
          <p>{{conteudo}}</p>
          <span>{{date}}</span>
        </div>
        {{/each}}
      </div>
    </div>
  </div>
</body>
<script src="js/search.js"></script>

</html>